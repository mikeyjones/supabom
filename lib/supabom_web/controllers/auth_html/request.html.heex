<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sign In - SupaBOM</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Fredoka:wght@300;400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <style>
      :root {
        --coral: #ff6b6b;
        --coral-bright: #ff8787;
        --mint: #51cf66;
        --mint-bright: #69db7c;
        --yellow: #ffd43b;
        --yellow-bright: #ffe066;
        --purple: #9775fa;
        --purple-bright: #b197fc;
        --dark-bg: #1a1625;
        --dark-surface: #251e35;
        --dark-elevated: #2f2640;
        --text: #f8f9fa;
        --text-dim: #adb5bd;
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: 'Fredoka', sans-serif;
        background: var(--dark-bg);
        color: var(--text);
        overflow-x: hidden;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 30px;
      }

      .animated-bg {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: 0;
        pointer-events: none;
        overflow: hidden;
      }

      .floating-shape {
        position: absolute;
        opacity: 0.08;
        filter: blur(60px);
        animation: gentleFloat 25s ease-in-out infinite;
      }

      .shape-1 {
        width: 300px;
        height: 300px;
        background: var(--coral);
        border-radius: 63% 37% 54% 46% / 55% 48% 52% 45%;
        top: 10%;
        left: 5%;
        animation-delay: 0s;
      }

      .shape-2 {
        width: 250px;
        height: 250px;
        background: var(--mint);
        border-radius: 30% 70% 70% 30% / 30% 30% 70% 70%;
        top: 60%;
        right: 10%;
        animation-delay: 5s;
      }

      .shape-3 {
        width: 280px;
        height: 280px;
        background: var(--purple);
        border-radius: 41% 59% 42% 58% / 41% 44% 56% 59%;
        bottom: 15%;
        left: 15%;
        animation-delay: 10s;
      }

      @keyframes gentleFloat {
        0%, 100% {
          transform: translate(0, 0) rotate(0deg) scale(1);
        }
        33% {
          transform: translate(30px, -30px) rotate(120deg) scale(1.1);
        }
        66% {
          transform: translate(-20px, 20px) rotate(240deg) scale(0.9);
        }
      }

      .auth-container {
        position: relative;
        z-index: 1;
        max-width: 480px;
        width: 100%;
      }

      .logo-link {
        display: block;
        text-align: center;
        text-decoration: none;
        margin-bottom: 30px;
      }

      .logo {
        font-size: 40px;
        font-weight: 700;
        background: linear-gradient(135deg, var(--coral) 0%, var(--yellow) 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        filter: drop-shadow(0 0 10px rgba(255, 107, 107, 0.5));
        display: inline-block;
        transform: rotate(-1deg);
        transition: transform 0.3s ease;
      }

      .logo:hover {
        transform: rotate(1deg) scale(1.05);
      }

      .form-bubble {
        background: var(--dark-surface);
        padding: 50px;
        border-radius: 40px;
        border: 4px solid var(--coral);
        box-shadow: 0 20px 60px rgba(0, 0, 0, 0.7), 0 0 60px rgba(255, 107, 107, 0.3);
        transform: rotate(-0.5deg);
        transition: all 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
        position: relative;
      }

      .form-bubble::before {
        content: '';
        position: absolute;
        bottom: -20px;
        left: 60px;
        width: 0;
        height: 0;
        border-left: 20px solid transparent;
        border-right: 20px solid transparent;
        border-top: 20px solid var(--dark-surface);
        filter: drop-shadow(0 4px 10px rgba(0, 0, 0, 0.5));
      }

      .form-bubble:hover {
        transform: rotate(0deg) translateY(-5px);
        box-shadow: 0 25px 70px rgba(0, 0, 0, 0.8), 0 0 80px rgba(255, 107, 107, 0.4);
      }

      h1 {
        font-size: 36px;
        font-weight: 700;
        margin-bottom: 12px;
        color: var(--text);
        text-align: center;
      }

      .subtitle {
        font-size: 16px;
        color: var(--text-dim);
        text-align: center;
        margin-bottom: 35px;
        font-weight: 500;
      }

      .info-box {
        background: rgba(151, 117, 250, 0.1);
        border: 2px solid var(--purple);
        border-radius: 20px;
        padding: 20px;
        margin-bottom: 25px;
        font-size: 15px;
        line-height: 1.6;
        color: var(--text-dim);
      }

      .form-group {
        margin-bottom: 25px;
      }

      label {
        display: block;
        font-size: 14px;
        font-weight: 600;
        color: var(--text);
        margin-bottom: 10px;
      }

      input[type="email"] {
        width: 100%;
        padding: 16px 20px;
        font-family: 'Fredoka', sans-serif;
        font-size: 16px;
        font-weight: 500;
        background: var(--dark-elevated);
        border: 3px solid transparent;
        border-radius: 30px;
        color: var(--text);
        transition: all 0.3s ease;
      }

      input[type="email"]:focus {
        outline: none;
        border-color: var(--coral);
        box-shadow: 0 0 30px rgba(255, 107, 107, 0.3);
        transform: scale(1.02);
      }

      input::placeholder {
        color: var(--text-dim);
      }

      .btn {
        width: 100%;
        padding: 18px 45px;
        font-family: 'Fredoka', sans-serif;
        font-size: 18px;
        font-weight: 700;
        border-radius: 30px;
        text-decoration: none;
        transition: all 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
        border: 4px solid transparent;
        cursor: pointer;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        line-height: 1;
        background: var(--coral);
        color: white;
        box-shadow: 0 0 30px rgba(255, 107, 107, 0.5);
        border-color: var(--coral-bright);
        transform: rotate(-0.5deg);
      }

      .btn:hover {
        background: var(--coral-bright);
        transform: rotate(0.5deg) scale(1.05) translateY(-3px);
        box-shadow: 0 0 50px rgba(255, 107, 107, 0.8);
      }

      .btn:active {
        transform: rotate(0deg) scale(0.98);
      }

      .links {
        margin-top: 30px;
        text-align: center;
      }

      .link {
        display: block;
        color: var(--mint-bright);
        text-decoration: none;
        font-size: 15px;
        font-weight: 600;
        margin-bottom: 12px;
        transition: all 0.3s ease;
      }

      .link:hover {
        color: var(--mint);
        transform: translateX(5px);
        display: inline-block;
      }

      .alert {
        padding: 15px 20px;
        border-radius: 20px;
        margin-bottom: 25px;
        font-size: 15px;
        font-weight: 600;
        text-align: center;
      }

      .alert-error {
        background: rgba(255, 107, 107, 0.2);
        border: 2px solid var(--coral);
        color: var(--coral-bright);
      }

      .alert-info {
        background: rgba(81, 207, 102, 0.2);
        border: 2px solid var(--mint);
        color: var(--mint-bright);
      }
    </style>
  </head>
  <body>
    <div class="animated-bg">
      <div class="floating-shape shape-1"></div>
      <div class="floating-shape shape-2"></div>
      <div class="floating-shape shape-3"></div>
    </div>

    <div class="auth-container">
      <a href="/" class="logo-link">
        <div class="logo">SupaBOM ✨</div>
      </a>

      <div class="form-bubble">
        <h1>Welcome! 👋</h1>
        <p class="subtitle">Sign in with a magic link</p>

        <%= if @flash["error"] do %>
          <div class="alert alert-error">
            {@flash["error"]}
          </div>
        <% end %>

        <%= if @flash["info"] do %>
          <div class="alert alert-info">
            {@flash["info"]}
          </div>
        <% end %>

        <.form
          for={@conn}
          action={~p"/auth/user/magic_link/request"}
          method="post"
          id="magic-link-form"
        >
          <div class="form-group">
            <label for="user_email">Email</label>
            <input
              type="email"
              name="user[email]"
              id="user_email"
              placeholder="your@email.com"
              required
              autocomplete="email"
            />
          </div>

          <button type="submit" class="btn">
            Send Magic Link 🚀
          </button>
        </.form>

        <div class="links">
          <a href="/" class="link">
            ← Back to home
          </a>
        </div>
      </div>
    </div>
  </body>
</html>
